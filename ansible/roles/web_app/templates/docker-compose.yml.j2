services:
  web:
    image: "{{ web_app_image }}"
    container_name: "{{ web_app_container_name }}"
    ports:
      - "{{ web_app_host_port }}:{{ web_app_container_port }}"
    restart: unless-stopped
